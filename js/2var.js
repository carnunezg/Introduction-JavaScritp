// Variables: var(ya no se usa en ES06) Ahora se usa el let y const

// VAR: ES LA FORMA DE ESCRIBIR EN JAVASCRIPT.

var producto = 'Audifonos Gamer'; // Iniciar variable y asignarle valor

var disponible; // iniciamos variables pero sin valor

// producto = true; // Reasignamos el valor de la variable

disponible = true; // se le asigna valor a la variable, la cual no tenia 

// NOTA: el var solo se le puede asignar una solo vez a la variable que ya tiene valor, si quires cambiar el valos, solo llama la variale y el nuevo valor

var producto1 = 'Computadora',
    disponible1 = 'true',
    categoria = 'Computadoras'; // esto me sirve para darles los valores a las variable sin estar llamando var a cada variale, solo crea var y nombre=valos seguido con una , y al final cierro con ;

//las variables no debe llevar al inicio numeros y - porque dará error en la consola de js. ejemplo var 1disponible; o var -disponible;

// Estilos para las variables
var nombre_producto = 'Minitor HD';// underscore
var nombreProducto = 'Minitor HD'; // Calmecase ESTA ES LA QUE VAMOS A USAR.
var PombreProducto = 'Minitor HD'; // Pascal Case 
var nombreproducto = 'Minitor HD'; // underscore

console.log(producto); // ES IMPORTANTE que al llamar la variable tiene que ser exacto respetando mayusculas y minusculas.

 