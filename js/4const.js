// Variables con const

// const: ES LA FORMA DE ESCRIBIR EN JAVASCRIPT.

const producto = 'Audifonos Gamer'; // Iniciar variable y asignarle valor

const disponible = true; // iniciamos variables pero sin valor

// producto = true; // Reasignamos el valor de la variable

//disponible = true; // en const no se le reasigna valor a la variable, es una sola vez.

// NOTA: el const siempre tiene que tener valor, porque dará error, ademas const no se puede reasignar valor.

const producto1 = 'Computadora',
    disponible1 = 'true',
    categoria = 'Computadoras'; // esto me sirve para darles los valores a las variable sin estar llamando const a cada variale, solo crea var y nombre=valos seguido con una , y al final cierro con ;

//las variables no debe llevar al inicio numeros y - porque dará error en la consola de js. ejemplo const 1disponible; o var -disponible;

// Estilos para las variables
const nombre_producto = 'Minitor HD';// underscore
const nombreProducto = 'Minitor HD'; // Calmecase ESTA ES LA QUE VAMOS A USAR.
const PombreProducto = 'Minitor HD'; // Pascal Case 
const nombreproducto = 'Minitor HD'; // underscore

