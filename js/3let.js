// Variables con let

// LET: ES LA FORMA DE ESCRIBIR EN JAVASCRIPT.

let producto = 'Audifonos Gamer'; // Iniciar variable y asignarle valor

let disponible; // iniciamos variables pero sin valor

// producto = true; // Reasignamos el valor de la variable

disponible = true; // se le asigna valor a la variable, la cual no tenia 

// NOTA: el let solo se le puede asignar una solo vez a la variable que ya tiene valor, si quires cambiar el valos, solo llama la variale y el nuevo valor

let producto1 = 'Computadora',
    disponible1 = 'true',
    categoria = 'Computadoras'; // esto me sirve para darles los valores a las variable sin estar llamando let a cada variale, solo crea var y nombre=valos seguido con una , y al final cierro con ;

//las variables no debe llevar al inicio numeros y - porque dará error en la consola de js. ejemplo let 1disponible; o var -disponible;

// Estilos para las variables
let nombre_producto = 'Minitor HD';// underscore
let nombreProducto = 'Minitor HD'; // Calmecase ESTA ES LA QUE VAMOS A USAR.
let PombreProducto = 'Minitor HD'; // Pascal Case 
let nombreproducto = 'Minitor HD'; // underscore

