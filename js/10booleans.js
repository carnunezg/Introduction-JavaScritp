//Booleans

 const boolean1 = true;
 const boolean2 = false;
 const boolean3 = "true";

 console.log(boolean1);
 console.log(boolean2);
 console.log(boolean3);

 // los booleans son aquellos que no llevan comillas, ya que la consolo lo identificara como un string(cadena) o un texto, y en la consola se vera con color negro y con eso identificamos que no es un booleans.

 // PARA SABER SI SON BOOLEANS O NO PODEMOS VERLO CON UN typeof EJEMPLO:
 console.log(typeof boolean1); //respuesta: boolean
 console.log(typeof boolean2); //respuesta: boolean
 console.log(typeof boolean3); //respuesta: string

// Los booleans me va a servir para saber si el usuario esta autenticado o no, el producto existe o son amigo en facebook, si son amigo (true) le muestro datos sino no le permito acceso (false)



